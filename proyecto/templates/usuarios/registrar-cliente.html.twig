{% extends 'layout/base.html.twig' %}

{% block content %}
    <div class="card shadow-sm rounded-lg mx-auto" style="max-width: 600px; width: 90vw; margin-top: 60px;">
        <div class="card-header bg-primary text-white">
            <h4 class="mb-0"><i class="ri-user-add-line me-2"></i> Registro de Cliente</h4>
        </div>
        <div class="card-body">
            {{ form_start(form) }}
                <div class="form-group mb-3">
                    {{ form_label(form.nombre, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.nombre, {'attr': {'class': 'form-control', 'placeholder': 'Ingrese nombre de cliente...'}}) }}
                </div>
                <div class="form-group mb-3">
                    {{ form_label(form.apellido, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.apellido, {'attr': {'class': 'form-control', 'placeholder': 'Ingrese apellido de cliente...'}}) }}
                </div>
                <div class="form-group mb-3">
                    {{ form_label(form.email, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.email, {'attr': {'class': 'form-control', 'placeholder': 'Ingrese correo electrónico de cliente...'}}) }}
                </div>
                <div class="form-group mb-3">
                    {{ form_label(form.dni, null, {'label_attr': {'class': 'form-label'}}) }}
                    {{ form_widget(form.dni, {'attr': {'class': 'form-control', 'placeholder': 'Ingrese email de cliente...'}}) }}
                </div>
                <div class="mt-4 d-flex justify-content-between">
                    <button type="submit" class="btn btn-success">
                        <i class="ri-save-line me-1"></i> Guardar
                    </button>
                    <a href="{{ path('app_usuarios_nuevo_cliente') }}" class="btn btn-secondary">
                        <i class="ri-arrow-go-back-line me-1"></i> Cancelar
                    </a>
                </div>
            {{ form_end(form, { render_rest: false }) }}
        </div>
    </div>
{% endblock %}